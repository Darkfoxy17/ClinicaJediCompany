@model WebAppChamadosTI.Models.Agendamento

@{
    ViewData["Title"] = "Excluir";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="d-flex justify-content-center mt-5">
    <div class="col-md-6 border rounded shadow-sm bg-white p-4 text-center">
        <h2 class="mb-4">Confirmação de Exclusão</h2>

        <div class="alert alert-danger">
            Deseja realmente excluir esta consulta?
        </div>

        <form asp-action="Excluir" method="post" class="mt-3">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn btn-danger me-2">Excluir</button>
            <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
        </form>
    </div>
</div>
